# P-Code ML API — Render.com / local Flask
# Install: pip install -r requirements.txt
# Run local:  python app.py
# Run Render: gunicorn app:app

Flask>=3.0.0,<4
flask-cors>=4.0.0,<5
gunicorn>=22.0.0,<24

# MySQL (Clever Cloud / Render)
PyMySQL>=1.1.0,<2
DBUtils>=3.0.0,<4
bcrypt>=4.0.0,<5

# Numerical / tabular (XGBoost path)
numpy>=1.26.0,<3
pandas>=2.0.0,<3
xgboost>=2.0.0,<4
scikit-learn>=1.3.0,<2
shap>=0.44.0,<0.50
scipy>=1.11.0,<2
joblib>=1.3.0,<2

# Imaging (CNN path) — hosted Render uses TFLite (low RAM)
# Linux/Render: tflite-runtime only. Windows/dev: full TensorFlow (includes tf.lite).
tflite-runtime>=2.14.0; platform_system != "Windows"
tensorflow>=2.15.0,<2.21; platform_system == "Windows"
Pillow>=10.0.0,<12
opencv-python-headless>=4.8.0,<5
